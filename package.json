{
  "name": "slapec.hu",
  "version": "1.0.0",
  "description": "Slapec's website",
  "author": "Peter Schon",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=master --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.4.3"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
